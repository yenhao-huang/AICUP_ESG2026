# Submit-only environment for machines whose NVIDIA driver cannot run CUDA 13
# wheels. This keeps the final prediction pipeline on PyTorch CUDA 12.4 while
# leaving the full research/training requirements.txt untouched.
#
# Install from the repository root:
#   python3 -m venv .venv
#   . .venv/bin/activate
#   pip install -U pip
#   pip install -r requirements-submit-cu124.txt

--extra-index-url https://download.pytorch.org/whl/cu124

torch==2.6.0+cu124
torchvision==0.21.0+cu124

accelerate>=1.1,<2
bitsandbytes>=0.46,<0.50
datasets>=3,<6
gdown>=5,<7
huggingface_hub>=0.30,<2
numpy>=1.26,<3
openai>=1.80,<3
pandas>=2.2,<4
peft>=0.15,<0.20
protobuf>=4.25,<7
PyYAML>=6.0,<7
requests>=2.32,<3
safetensors>=0.4,<1
scikit-learn>=1.5,<2
scipy>=1.13,<2
sentence-transformers>=3,<6
tokenizers>=0.21,<0.23
tqdm>=4.66,<5
transformers>=4.53,<5
trl>=0.18,<2
